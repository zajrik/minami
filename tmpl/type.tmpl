<?js
    var data = obj;
    var self = this;
    data.forEach(function(name, i) { ?>
<span class="param-type"><dd><?js= self.linkto(name, self.htmlsafe(name)) ?></dd></span>
<?js if (i < data.length-1) { ?>|<?js } ?>
<?js }); ?>
